# Repository agent instructions

## 프로젝트 개요

- Java 21, Spring Boot 4 기반의 멀티 모듈 Gradle 프로젝트입니다.
- 실행 서비스는 `api-gateway`, `auth-server`, `musical`, `payment`, `queue`, `ticketing`입니다.
- 여러 서비스가 함께 사용하는 코드는 `common`, 공통 로깅과 메트릭 설정은 `common-observability`에 둡니다.
- 로컬 인프라는 `docker-compose.infra.yml`, 전체 애플리케이션은 `docker-compose.yml`을 기준으로 실행합니다.

## 작업 원칙

- 동작을 변경하기 전에 대상 모듈의 `build.gradle`과 관련 `application*.yml`을 확인합니다.
- 서비스 경계를 유지하고 다른 서비스의 데이터베이스나 Repository에 직접 접근하지 않습니다.
- 작업에서 명시하지 않은 공개 API, Kafka 이벤트 계약, 설정 키는 유지합니다.
- 두 개 이상의 모듈에서 실제로 공유하는 코드만 `common`에 추가합니다.
- 자격 증명, 토큰, 개인 키, 실제 환경별 비밀값은 커밋하지 않습니다.
- 사용자의 기존 변경을 보존하고 요청 범위 밖의 파일은 수정하지 않습니다.

## 검증

- 가장 작은 관련 테스트부터 실행합니다.
  - Windows: `.\gradlew.bat :<module>:test`
  - Unix 계열: `./gradlew :<module>:test`
- `common`, 공통 빌드 설정, 서비스 간 계약을 변경하면 영향받는 모든 모듈을 검증합니다.
- 영향 범위가 넓으면 `.\gradlew.bat test` 또는 `./gradlew test`로 전체 테스트를 실행합니다.
- 검증에 필요한 경우가 아니면 Docker 인프라를 실행하지 않습니다.
- 작업 완료 시 변경 내용, 실행한 검증, 실행하지 못한 검증을 함께 보고합니다.

## 코드 리뷰 지침

- 모든 리뷰 결과는 한국어로 작성합니다.
- 요약보다 실제 장애나 회귀로 이어질 수 있는 발견 사항을 먼저 제시하고, 심각도와 파일 위치를 포함합니다.
- 스타일 선호만 지적하지 말고 재현 가능한 동작, 실패 시나리오, 계약 위반, 테스트 공백에 집중합니다.
- 변경된 파일만 보지 말고 호출 방향, 데이터 소유권, 서비스 경계, 배포 순서를 포함한 전체 구조에서 검토합니다.
- 다른 서비스의 데이터나 내부 구현에 직접 의존해 결합도가 증가하지 않는지 확인합니다.
- 동기 호출 체인이 길어지거나 timeout, retry, circuit breaker, backpressure 부재로 장애가 전파되지 않는지 확인합니다.
- Kafka 이벤트의 스키마 호환성, partition key, 순서 보장 범위, 중복 소비, 멱등성, 재시도, DLQ, outbox 필요성을 확인합니다.
- JPA 연관관계, aggregate 경계, N+1, cascade, fetch 전략, locking과 트랜잭션 범위가 의도한 정합성을 보장하는지 확인합니다.
- 외부 호출이 긴 DB 트랜잭션에 포함되거나 부분 실패로 데이터가 불일치할 가능성을 확인합니다.
- API, 이벤트, 데이터베이스 변경의 하위 호환성, 점진 배포 가능성, rollback 가능성을 확인합니다.
- 현재 프로젝트는 실제 운영·배포 중인 서비스가 아니라 개발 중인 시스템입니다. 사용자가 별도로 운영 전환이나 데이터 보존을 요구하지 않는 한, 기존 운영 데이터 백필, 활성 Redis 키·세션 호환, 구·신 버전 인스턴스가 공존하는 롤링 배포만을 전제로 한 지적은 리뷰 발견 사항으로 제시하지 않습니다.
- 사용자가 운영 환경, 무중단 배포, 기존 데이터 유지, rollback 호환성을 명시한 작업에서는 위 항목을 다시 필수로 검토합니다.
- 로그의 추적 식별자, 개인정보 노출, 메트릭 cardinality, 핵심 실패 지표와 알람 가능성을 확인합니다.
- 정상 경로뿐 아니라 동시성, 중복 메시지, 순서 역전, timeout, 부분 장애를 검증하는 테스트가 있는지 확인합니다.

## 에이전트 활용 지침

- 작은 단일 모듈 변경은 기본 에이전트가 직접 처리하고 불필요하게 서브에이전트를 생성하지 않습니다.
- 두 개 이상의 서비스에 영향을 주거나 API·Kafka·DB 계약, 트랜잭션, 동시성, 인프라 설정을 변경하는 작업은 관련 커스텀 에이전트에 독립 검토를 위임하고 결과를 통합합니다.
- 설계 검토는 `backend_architect`, 구현은 `backend_implementer`, 테스트 분석과 보완은 `backend_test_engineer`, 인프라와 관측성은 `platform_engineer`, 분산 시스템 심층 리뷰는 `distributed_system_reviewer`를 사용합니다.
- 서로 같은 파일을 수정할 가능성이 있는 쓰기 에이전트는 병렬 실행하지 않습니다. 탐색과 리뷰처럼 읽기 전용 작업만 우선 병렬화합니다.
- 최종 범위 결정, 충돌 해결, 검증 결과 통합은 주 에이전트가 책임집니다.

## 문서와 작업 기록

- 장기적으로 유지할 시스템 구조는 `docs/architecture/`에 기록합니다.
- 기능별 기술 설계는 `docs/trd/`, 중요한 기술 결정은 `docs/adr/`에 기록합니다.
- 현재 작업의 구현 계획은 `.agents/plans/`, 다른 스레드로 넘길 진행 상황은 `.agents/handoffs/`에 기록합니다.
- Codex가 자동으로 읽는 저장소 지침은 이 `AGENTS.md`입니다. 다른 문서는 작업에 필요할 때 명시적으로 읽습니다.
